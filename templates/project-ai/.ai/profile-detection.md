# Profile Detection

> 本文件由 `archsight-aios init` 首次创建时自动生成。
> 它根据本地项目名、README、package / pyproject 和浅层文件名推断可用 profile 与候选 Skill，目的是降低接入门槛，不替代人工确认。

## 识别结论

- 模式：`auto`
- 当前启用 profile：待自动生成
- 规则来源：ArchSight AIOS 内置 profile registry。

## Profile 命中证据

- 待自动生成。

## Skill 候选

- 待自动生成。

## 边界

- 自动识别只用于初始化和路由建议，不是业务事实或工程结论。
- 未命中 profile 时，仍可按用户任务和资料内容触发对应 `aios-*` Skill。
- 涉及金额、工期、法律责任、结构安全、规范合规或人员信息时，必须保留人工复核。
