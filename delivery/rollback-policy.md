# Rollback Policy

## 目标

每次交付都必须知道如何撤回。回滚策略是发布条件，不是事故后补救。

## 回滚方式

| 类型 | 推荐方式 |
| --- | --- |
| 文档 / Prompt / Workflow | Git revert |
| CLI / 脚本 | 回退版本或撤回发布 |
| 配置 | 恢复上一版配置并记录差异 |
| 数据迁移 | 使用预先验证的反向迁移或备份恢复 |

## 记录字段

```text
变更：
版本 / commit：
触发条件：
回滚命令：
验证方式：
负责人：
```

